version: "3.9"
services:
  rag-chatbot:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - chroma_store:/app/chroma_store
      - logs:/app/logs
volumes:
  chroma_store:
  logs:
