# Superseded -- this workflow now lives at .github/workflows/ci.yml, which
# is where GitHub Actions actually reads it from. This root-level copy is
# kept only so a diff of this commit shows what changed; delete it whenever
# you're comfortable doing so, it has no effect on CI from here.
